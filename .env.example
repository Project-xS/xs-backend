DATABASE_URL=

# Firebase Auth
FIREBASE_PROJECT_ID=
# Optional override for JWKS/X.509 endpoint used to verify Firebase ID tokens
# Default (if unset): https://www.googleapis.com/service_accounts/v1/jwk/securetoken@system.gserviceaccount.com
# You may also set it to the X.509 certs endpoint and it will work as well:
#   https://www.googleapis.com/robot/v1/metadata/x509/securetoken@system.gserviceaccount.com
FIREBASE_JWKS_URL=
# Optional comma separated allowed domains (e.g., example.com,example.org)
ALLOWED_GOOGLE_DOMAINS=

# Admin JWT
ADMIN_JWT_SECRET=
ADMIN_JWT_ISSUER=canteen-auth
ADMIN_JWT_AUDIENCE=admin
# Optional, defaults to 12h
ADMIN_JWT_EXPIRY_SECS=

# Swagger UI Basic Auth (optional; if set, UI requires Basic auth)
SWAGGER_BASIC_USERNAME=
SWAGGER_BASIC_PASSWORD=
RUST_LOG=

# Dev-only auth bypass (debug builds only, ignored in release).
# Set a token value and use it as Bearer token in Postman.
# Append ?as=user-{id} or ?as=admin-{id} to control the principal.
# Example: Bearer my-dev-token with ?as=admin-1 → Admin(canteen_id=1)
DEV_BYPASS_TOKEN=
S3_ACCESS_KEY_ID=
S3_SECRET_KEY=
S3_ENDPOINT=
S3_BUCKET_NAME=
S3_REGION=

# Order Hold / QR
DELIVER_QR_HASH_SECRET=
# Optional, defaults to 300 (5 minutes)
ORDER_HOLD_TTL_SECS=
# Optional, defaults to 86400 (24 hours)
QR_TOKEN_MAX_AGE_SECS=
